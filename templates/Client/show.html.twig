{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ client.Prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ client.Adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ client.Tel }}</td>
            </tr>
            <tr>
                <th>NumClient</th>
                <td>{{ client.NumClient }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_controller2_index') }}">back to list</a>

    <a href="{{ path('app_client_controller2_edit', {'id': client.id}) }}">edit</a>

    {{ include('Client/_delete_form.html.twig') }}
{% endblock %}
