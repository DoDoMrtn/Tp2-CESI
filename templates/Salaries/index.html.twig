{% extends 'base.html.twig' %}

{% block title %}Liste des Salariés{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center">
            <button onclick="history.back()" class="btn btn-secondary">Retour</button>
            <h1 class="text-center">Liste des Salariés</h1>
            <div style="width: 800px"></div>
        </div>

        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Matricule</th>
                    <th scope="col">Nom</th>
                    <th scope="col">Prenom</th>
                    <th scope="col">Departement</th>
                    <th scope="col">Poste</th>
                    <th scope="col">Salaire</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for salarie in salaries %}
                    <tr>
                        <td>{{ salarie.id }}</td>
                        <td>{{ salarie.Matricule }}</td>
                        <td>{{ salarie.Nom }}</td>
                        <td>{{ salarie.Prenom }}</td>
                        <td>{{ salarie.Departement }}</td>
                        <td>{{ salarie.Poste }}</td>
                        <td>{{ salarie.Salaire }}</td>
                        <td>
                            <a href="{{ path('app_salarie_controller2_edit', {'id': salarie.id}) }}" class="btn btn-primary">Éditer</a>
                            <a href="{{ path('app_salarie_controller2_delete', {'id': salarie.id}) }}" class="btn btn-danger">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_salarie_controller2_new') }}" class="btn btn-success">Ajouter un salarié</a>
    </div>
{% endblock %}