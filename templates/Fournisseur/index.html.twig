{% extends 'base.html.twig' %}

{% block title %}Liste des Fournisseurs{% endblock %}

{% block body %}
    <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center">
        <button onclick="history.back()" class="btn btn-secondary">Retour</button>
        <h1 class="text-center">Liste des Fournisseurs</h1>
        <div style="width: 800px"></div>
    </div>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Num</th>
                    <th scope="col">Liste Produits</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for fournisseur in fournisseurs %}
                    <tr>
                        <td>{{ fournisseur.id }}</td>
                        <td>{{ fournisseur.numFournisseur }}</td>
                        <td>{{ fournisseur.produits }}</td>

                        <td>
                            <a href="#" class="btn btn-primary">Éditer</a>
                            <a href="#" class="btn btn-danger">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <a href="#" class="btn btn-success">Ajouter un fournisseur</a>
    </div>
{% endblock %}