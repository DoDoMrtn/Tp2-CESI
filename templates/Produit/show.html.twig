{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ produit.Reference }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.Nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.Prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_controller2_index') }}">back to list</a>

    <a href="{{ path('app_produit_controller2_edit', {'id': produit.id}) }}">edit</a>

    {{ include('Produit/_delete_form.html.twig') }}
{% endblock %}
